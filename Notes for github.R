# Notes for git
# 1. If git is empty, you can pull to verify if already is to date.
# 2. Make sure you open k new file every time you practice and save it with  specific title
# 3. find the doc with problem --> copy it somewhere safe --> Right click --> revert the file that is causing problem --> get the version of the doc that works --> and then push it